import Card from "../../components/UI/Card";
import AuthForm from "../../components/AuthForm/AuthForm";

const Auth = () => {
  return (
    <Card>
      <AuthForm />
    </Card>
  );
};

export default Auth;
